# 🚀 즉시 실행 - Elementor 라이선스 DB 정리

---

## ⚡ **방법 선택 (난이도 순)**

### 🟢 **LEVEL 1: 자동화 (추천) - 클릭 한 번**

Windows PowerShell에서:

```powershell
.\generate-mysql-command.ps1
```

→ **SSH 정보 입력만 하면 자동으로 처리됨 ✅**

---

### 🟡 **LEVEL 2: 스크립트 업로드 - SSH 접속 필요**

```bash
# 1. 로컬 → 서버 업로드 (Windows PowerShell에서)
scp generate-mysql-command.sh [사용자명]@[서버IP]:/tmp/

# 2. SSH 접속
ssh [사용자명]@[서버IP]

# 3. 워드프레스 경로로 이동
cd /home/master/applications/[앱이름]/public_html

# 4. 스크립트 실행
bash /tmp/generate-mysql-command.sh
```

---

### 🔴 **LEVEL 3: 완전 수동 - 복사 & 붙여넣기**

#### STEP 1: SSH 접속 & DB 정보 확인
```bash
ssh [사용자명]@[서버IP]
cd /home/master/applications/[앱이름]/public_html
grep -E "DB_NAME|DB_USER|DB_PASSWORD|DB_HOST" wp-config.php
```

#### STEP 2: 아래 명령어에 정보 대입 후 실행

```bash
mysql -h"[호스트]" -u"[사용자]" -p"[비밀번호]" "[DB명]" -e "DELETE FROM wp_options WHERE option_name IN ('elementor_pro_license_key', '_elementor_pro_license_data', 'elementor_remote_info_library'); SELECT option_name FROM wp_options WHERE option_name LIKE '%elementor%license%';"
```

**예시:**
```bash
mysql -h"localhost" -u"myuser" -p"mypass123" "mydatabase" -e "DELETE FROM wp_options WHERE option_name IN ('elementor_pro_license_key', '_elementor_pro_license_data', 'elementor_remote_info_library'); SELECT option_name FROM wp_options WHERE option_name LIKE '%elementor%license%';"
```

---

## 🎯 **초간단 복사용 템플릿**

### 📋 **변수 설정 후 실행 (가장 깔끔)**

```bash
# 1. SSH 접속 후 변수 설정
DB_NAME="mydatabase"
DB_USER="myuser"
DB_PASSWORD="mypassword"
DB_HOST="localhost"

# 2. One-Liner 실행
mysql -h"$DB_HOST" -u"$DB_USER" -p"$DB_PASSWORD" "$DB_NAME" -e "DELETE FROM wp_options WHERE option_name IN ('elementor_pro_license_key', '_elementor_pro_license_data', 'elementor_remote_info_library'); SELECT option_name FROM wp_options WHERE option_name LIKE '%elementor%license%';"
```

---

## ✅ **성공 확인**

실행 후 아래와 같이 출력되면 성공:

```
Query OK, 3 rows affected (0.01 sec)
Empty set (0.00 sec)
```

→ **워드프레스 관리자 페이지 접속 → Elementor 편집기 열기 → Safe Mode 해제됨!**

---

## 🆘 **문제 해결**

| 에러 메시지 | 해결 방법 |
|-------------|----------|
| `Access denied` | 비밀번호 재확인 |
| `Command not found: mysql` | WP-CLI 사용: `wp option delete` |
| `Table doesn't exist` | 테이블 접두사 확인 (`$table_prefix`) |
| `404 Error (PHP)` | ✅ 이미 해결됨 (터미널 방식 사용 중) |

---

## 📞 **빠른 지원**

**상세 가이드:** `ONE_LINER_명령어_가이드.md` 열기  
**전체 매뉴얼:** `TERMINAL_DB_CLEANUP_COMMANDS.md` 참고  

---

**추천 순서:**  
1️⃣ PowerShell 자동화 시도  
2️⃣ 실패 시 LEVEL 3 수동 실행  
3️⃣ 여전히 안되면 WP-CLI 사용 (`wp option delete`)

**작업 시간:** 1-2분 ⚡







