# 🔥 Elementor 안전 모드 완전 제거 (최종판)

## 🎯 재설치 전에 이것부터!

재설치는 시간이 오래 걸리고 설정을 잃을 수 있습니다.
아래 방법을 **순서대로** 시도하세요. 90% 확률로 해결됩니다!

---

## ⚡ 방법 1: 데이터베이스에서 안전 모드 완전 제거 (가장 강력!)

### 1단계: phpMyAdmin 접속

**호스팅 제공업체 제어판(cPanel):**
```
1. 호스팅 업체 웹사이트 로그인
2. cPanel 또는 제어판 접속
3. "데이터베이스" 섹션 찾기
4. "phpMyAdmin" 클릭
```

### 2단계: WordPress 데이터베이스 선택

왼쪽 목록에서 WordPress 데이터베이스 클릭:
```
보통: wp_something, wordpress_, cms_ 등으로 시작
```

### 3단계: SQL 탭에서 코드 실행

상단의 **"SQL"** 탭 클릭 후 아래 코드 **전체 복사 → 붙여넣기 → 실행**:

```sql
-- ============================================
-- Elementor 안전 모드 완전 제거 (데이터베이스)
-- ============================================

-- 1. 현재 안전 모드 상태 확인
SELECT 'BEFORE - User Meta', user_id, meta_key, meta_value 
FROM wp_usermeta 
WHERE meta_key LIKE '%elementor%safe%' OR meta_key LIKE '%safe%mode%';

SELECT 'BEFORE - Options', option_name, option_value 
FROM wp_options 
WHERE option_name LIKE '%elementor%safe%' OR option_name LIKE '%safe%mode%';

-- 2. 사용자 메타에서 안전 모드 플래그 완전 삭제
DELETE FROM wp_usermeta 
WHERE meta_key LIKE '%elementor%safe%mode%';

DELETE FROM wp_usermeta 
WHERE meta_key LIKE '_elementor_safe_mode%';

DELETE FROM wp_usermeta 
WHERE meta_key = 'elementor_safe_mode';

-- 3. 옵션 테이블에서 안전 모드 플래그 삭제
DELETE FROM wp_options 
WHERE option_name LIKE '%elementor%safe%mode%';

DELETE FROM wp_options 
WHERE option_name = 'elementor_safe_mode';

-- 4. 트랜지언트(캐시) 삭제
DELETE FROM wp_options 
WHERE option_name LIKE '_transient_elementor_safe_mode%';

DELETE FROM wp_options 
WHERE option_name LIKE '_transient_timeout_elementor_safe_mode%';

-- 5. 모든 사용자의 Elementor 세션 초기화
DELETE FROM wp_usermeta 
WHERE meta_key LIKE 'elementor_preferences%';

-- 6. 최종 확인 (삭제되었는지 확인)
SELECT 'AFTER - User Meta', user_id, meta_key, meta_value 
FROM wp_usermeta 
WHERE meta_key LIKE '%safe%mode%';

SELECT 'AFTER - Options', option_name, option_value 
FROM wp_options 
WHERE option_name LIKE '%safe%mode%';

-- 7. 결과 출력
SELECT '✅ 안전 모드 데이터 완전 삭제 완료!' AS Result;
```

**⚠️ 중요:** 테이블 접두사가 `wp_`가 아닐 수 있습니다!
- 왼쪽 목록에서 `_usermeta`와 `_options` 테이블 이름 확인
- 코드에서 `wp_`를 실제 접두사로 변경

### 4단계: 실행 결과 확인

```
✅ "Query OK" 또는 "쿼리 성공" 메시지
✅ 마지막에 "안전 모드 데이터 완전 삭제 완료!" 표시
```

---

## ⚡ 방법 2: 브라우저 완전 클린 (필수!)

### Chrome/Edge에서:

```
1. Ctrl + Shift + Delete
2. "전체 기간" 선택
3. 다음 항목 모두 체크:
   ✓ 쿠키 및 기타 사이트 데이터
   ✓ 캐시된 이미지 및 파일
   ✓ 호스팅된 앱 데이터
   ✓ 사이트 설정 (있다면)
4. "데이터 삭제" 클릭
```

### 또는 Incognito/시크릿 모드로 테스트:

```
Ctrl + Shift + N (Chrome)
Ctrl + Shift + P (Firefox)

→ https://cms.pnamarketing.co.kr/wp-admin 접속
→ Elementor 편집기 테스트
```

---

## ⚡ 방법 3: WordPress 플러그인으로 안전 모드 제거

### Code Snippets에 최강 코드 추가:

1. **WordPress 관리자 → Snippets → Add New**
2. **제목:** `안전 모드 강제 제거`
3. **코드 붙여넣기:**

```php
<?php
/**
 * Elementor 안전 모드 완전 차단 (최강 버전)
 */

// 1. 모든 안전 모드 쿠키 차단
add_action('init', function() {
    // 기존 쿠키 삭제
    $cookies_to_delete = [
        'elementor_safe_mode',
        'elementor-safe-mode',
        'elementor_safe_mode_cookie',
        'elementorSafeMode'
    ];
    
    foreach ($cookies_to_delete as $cookie) {
        if (isset($_COOKIE[$cookie])) {
            setcookie($cookie, '', time() - 3600, '/', '', true, true);
            setcookie($cookie, '', time() - 3600, '/', $_SERVER['HTTP_HOST'], true, true);
            setcookie($cookie, '', time() - 3600, '/', '.' . $_SERVER['HTTP_HOST'], true, true);
            unset($_COOKIE[$cookie]);
        }
    }
}, 1);

// 2. 데이터베이스에서 안전 모드 플래그 자동 삭제
add_action('admin_init', function() {
    global $wpdb;
    $user_id = get_current_user_id();
    
    // 사용자 메타 삭제
    $wpdb->query("DELETE FROM {$wpdb->usermeta} WHERE meta_key LIKE '%elementor%safe%mode%'");
    $wpdb->query("DELETE FROM {$wpdb->usermeta} WHERE meta_key LIKE '_elementor_safe_mode%'");
    
    // 옵션 삭제
    $wpdb->query("DELETE FROM {$wpdb->options} WHERE option_name LIKE '%elementor%safe%mode%'");
    
    // 트랜지언트 삭제
    $wpdb->query("DELETE FROM {$wpdb->options} WHERE option_name LIKE '_transient%elementor%safe%'");
}, 1);

// 3. Elementor 편집기 설정 강제 변경
add_filter('elementor/editor/localize_settings', function($settings) {
    // 안전 모드 강제 비활성화
    if (isset($settings['initial_document'])) {
        $settings['initial_document']['safe_mode'] = false;
    }
    if (isset($settings['document'])) {
        $settings['document']['safe_mode'] = false;
    }
    if (isset($settings['safe_mode'])) {
        $settings['safe_mode'] = false;
    }
    return $settings;
}, 999);

// 4. URL 파라미터 차단
add_action('admin_init', function() {
    if (isset($_GET['elementor-mode']) && $_GET['elementor-mode'] === 'safe') {
        wp_safe_redirect(remove_query_arg('elementor-mode'));
        exit;
    }
    if (isset($_GET['safe-mode'])) {
        wp_safe_redirect(remove_query_arg('safe-mode'));
        exit;
    }
}, 1);

// 5. JavaScript로 안전 모드 완전 차단
add_action('elementor/editor/before_enqueue_scripts', function() {
    ?>
    <script>
    (function() {
        'use strict';
        
        console.log('🔥 안전 모드 차단 스크립트 실행 중...');
        
        // 1. Elementor 설정 강제 변경
        if (typeof elementorCommon !== 'undefined') {
            elementorCommon.config = elementorCommon.config || {};
            elementorCommon.config.safe_mode = false;
            if (elementorCommon.config.document) {
                elementorCommon.config.document.safe_mode = false;
            }
        }
        
        if (typeof elementor !== 'undefined') {
            elementor.config = elementor.config || {};
            elementor.config.safe_mode = false;
            if (elementor.config.document) {
                elementor.config.document.safe_mode = false;
            }
        }
        
        // 2. 쿠키 설정 완전 차단
        const originalCookieDescriptor = Object.getOwnPropertyDescriptor(Document.prototype, 'cookie');
        if (originalCookieDescriptor) {
            Object.defineProperty(document, 'cookie', {
                get: function() {
                    return originalCookieDescriptor.get.call(document);
                },
                set: function(value) {
                    // 안전 모드 쿠키 차단
                    if (value.includes('elementor') && (value.includes('safe') || value.includes('Safe'))) {
                        console.log('🚫 안전 모드 쿠키 차단됨:', value);
                        return;
                    }
                    originalCookieDescriptor.set.call(document, value);
                }
            });
        }
        
        // 3. 로컬/세션 스토리지 안전 모드 데이터 삭제
        ['localStorage', 'sessionStorage'].forEach(function(storageType) {
            const storage = window[storageType];
            Object.keys(storage).forEach(function(key) {
                if (key.includes('elementor') && key.includes('safe')) {
                    storage.removeItem(key);
                    console.log('🗑️ 스토리지 삭제:', key);
                }
            });
        });
        
        // 4. URL 파라미터 제거
        if (window.location.href.includes('safe-mode') || window.location.href.includes('elementor-mode=safe')) {
            const url = new URL(window.location.href);
            url.searchParams.delete('safe-mode');
            url.searchParams.delete('elementor-mode');
            window.history.replaceState({}, '', url);
            console.log('🔧 URL 파라미터 제거됨');
        }
        
        console.log('✅ 안전 모드 차단 완료!');
    })();
    </script>
    <?php
}, 1);

// 6. 성공 알림
add_action('admin_notices', function() {
    if (isset($_GET['action']) && $_GET['action'] === 'elementor') {
        echo '<div class="notice notice-success is-dismissible">';
        echo '<p><strong>🔥 Elementor 안전 모드 차단 활성화됨!</strong></p>';
        echo '</div>';
    }
});
?>
```

4. **"Save Changes and Activate" 클릭**

---

## ⚡ 방법 4: Elementor CSS 파일 완전 재생성

### FTP 또는 파일 관리자로:

1. **WordPress 폴더 접속**
2. **다음 폴더로 이동:**
   ```
   /wp-content/uploads/elementor/css/
   ```
3. **폴더 안의 모든 파일 삭제** (폴더는 남겨두기!)
4. **권한 설정:** 폴더를 755로 설정
5. **Elementor 편집기 열기** → 자동으로 CSS 재생성됨

---

## ⚡ 방법 5: Elementor 플러그인 설정 리셋

### WP-CLI 사용 가능한 경우:

```bash
# SSH로 서버 접속 후
cd /path/to/wordpress

# Elementor 설정 완전 리셋
wp elementor flush-css

# 옵션 삭제
wp option delete elementor_safe_mode
wp user meta delete $(wp user list --field=ID) elementor_safe_mode
wp user meta delete $(wp user list --field=ID) _elementor_safe_mode
```

---

## ✅ 모든 방법 실행 후 테스트

### 1. 브라우저 완전 재시작
```
1. 모든 브라우저 창 닫기
2. 브라우저 다시 열기
3. Incognito 모드로 접속
```

### 2. WordPress 재로그인
```
https://cms.pnamarketing.co.kr/wp-admin
```

### 3. Elementor 편집기 열기
```
페이지 → 모든 페이지 → "Elementor로 편집" 클릭
```

### 4. 확인
```
✅ 안전 모드 배지 없음
✅ 좌측 위젯 패널 정상 표시
✅ 드래그 앤 드롭 작동
✅ CSS 편집 가능
```

---

## 🔄 여전히 안 되면: Elementor 재설치

위 모든 방법을 시도했는데도 안 되면 재설치를 진행합니다.

**다음 파일을 확인하세요:**
- `Elementor_완전_재설치_가이드.md`

---

## 📋 체크리스트

순서대로 체크하며 진행:

- [ ] 방법 1: 데이터베이스에서 안전 모드 플래그 삭제 (phpMyAdmin)
- [ ] 방법 2: 브라우저 캐시/쿠키 완전 삭제 + Incognito 테스트
- [ ] 방법 3: Code Snippets에 최강 안전 모드 차단 코드 추가
- [ ] 방법 4: CSS 파일 폴더 완전 삭제 후 재생성
- [ ] 브라우저 완전 재시작
- [ ] WordPress 재로그인
- [ ] Elementor 편집기 테스트

**모두 완료했는데도 안 되면 재설치를 진행합니다!**








